name: Run tests on project

on:
  push:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Setup repo
        uses: actions/checkout@v2

      - name: Setup Deno
        uses: denolib/setup-deno@v2
        with:
          deno-version: "v1.5.4"

      - name: Run tests
        run: TEST_ENVIRONMENT=true TEST_HOSTNAME="hattie.db.elephantsql.com" TEST_DATABASE="hvusjfns" TEST_USER="hvusjfns" TEST_PASSWORD="Qtuw2-Hy7WZbixjca5Mbk510uC7hUKCH" TEST_PORT="5432" deno test --allow-all --unstable
