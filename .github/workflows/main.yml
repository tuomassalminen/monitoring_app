name: Run tests on project

on:
  push:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Setup repo
        uses: actions/checkout@v2

      - name: Setup Deno
        uses: denolib/setup-deno@v2
        with:
          deno-version: "v1.5.4"

      - name: Run tests
        run: HOSTNAME="hattie.db.elephantsql.com" DATABASE="hvusjfns" USER="hvusjfns" PASSWORD="Qtuw2-Hy7WZbixjca5Mbk510uC7hUKCH" PORT="5432" deno test --allow-all --unstable
